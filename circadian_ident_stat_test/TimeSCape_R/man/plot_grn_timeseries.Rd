% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathway_circadian.R
\name{plot_grn_timeseries}
\alias{plot_grn_timeseries}
\title{Plot GRN across ZT time points as a multi-panel figure}
\usage{
plot_grn_timeseries(
  obj,
  circ_genes,
  pathway_genes,
  meta,
  celltype_col,
  zt_col,
  tmeta,
  target_ct,
  cor_thresh = 0.2,
  p_thresh = 0.05,
  use_norm = TRUE,
  outfile = NULL,
  ncol = NULL,
  node_size = 4,
  label_size = 4,
  edge_width_max = 2.5,
  zt_title_size = 18,
  zt_title_hjust = 0.5
)
}
\arguments{
\item{obj}{Seurat or SingleCellExperiment object.}

\item{circ_genes}{Character vector of confident circadian genes (from T1).}

\item{pathway_genes}{Character vector of pathway member genes to overlay.}

\item{meta}{data.frame of metadata (.get_meta(obj)).}

\item{celltype_col}{Metadata column for cell type.}

\item{zt_col}{Metadata column for ZT strings.}

\item{tmeta}{data.frame from build_tmeta().}

\item{target_ct}{Cell type to analyse.}

\item{cor_thresh}{Edge |r| threshold (default 0.2).}

\item{p_thresh}{Edge p-value threshold (default 0.05).}

\item{use_norm}{Use normalised slot (default TRUE).}

\item{outfile}{Path for output PNG (default NULL = not saved, only returned).}

\item{ncol}{Number of columns in the grid (default: auto).}

\item{node_size}{Node size in ggraph (default 4).}
}
\value{
A ggplot2 object (the arranged grid).
}
\description{
Builds one network per ZT time point using \code{.build_zt_network()}, then
arranges them in a grid.  A fixed spring layout is computed from the pooled
(all-ZT) network so node positions are consistent across panels.
}
