% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathway_circadian.R
\name{pathway_cosinor}
\alias{pathway_cosinor}
\title{Run cosinor on AUCell pathway scores for one cell type}
\usage{
pathway_cosinor(
  auc_mat,
  meta,
  celltype_col,
  zt_col,
  tmeta,
  target_ct,
  period12 = FALSE,
  custom_zt = NULL
)
}
\arguments{
\item{auc_mat}{Pathways × cells matrix from \code{auc_score_cells()}.}

\item{meta}{data.frame of cell-level metadata (.get_meta(obj)).}

\item{celltype_col}{Metadata column name for cell type.}

\item{zt_col}{Metadata column name for ZT strings.}

\item{tmeta}{data.frame from \code{build_tmeta()}.}

\item{target_ct}{Character. Cell type to analyse (must match celltype_col values).}

\item{period12}{Logical. 12-hr (TRUE) or 24-hr (FALSE, default).}

\item{custom_zt}{Optional character vector to restrict ZT time points.}
}
\value{
A list with:
  \describe{
    \item{stats}{data.frame — one row per pathway; same columns as run_timescape T1.}
    \item{zt_means}{data.frame — per-ZT mean AUC; pathways × ZT.}
  }
}
\description{
Treats each pathway's AUC score as if it were a "gene" and fits the same
cosinor model used by \code{estimate_phaseR}.  Returns a results table
comparable to \code{run_timescape}'s T1, plus per-ZT mean scores for heatmap.
}
