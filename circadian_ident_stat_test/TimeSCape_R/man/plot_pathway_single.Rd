% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathway_circadian.R
\name{plot_pathway_single}
\alias{plot_pathway_single}
\title{Plot a single pathway's AUC scores across ZT time points with cosine fit}
\usage{
plot_pathway_single(
  auc_mat,
  path_results,
  meta,
  celltype_col,
  zt_col,
  tmeta,
  target_ct,
  target_pathway,
  period12 = FALSE,
  use_violin = FALSE,
  custom_zt = NULL
)
}
\arguments{
\item{auc_mat}{Pathways × cells matrix from \code{auc_score_cells()}.}

\item{path_results}{List returned by \code{pathway_cosinor()} with \code{$stats} and \code{$zt_means}.}

\item{meta}{data.frame of cell-level metadata (\code{.get_meta(obj)}).}

\item{celltype_col}{Metadata column for cell type.}

\item{zt_col}{Metadata column for ZT strings.}

\item{tmeta}{data.frame from \code{build_tmeta()}.}

\item{target_ct}{Cell type name (must match celltype_col values).}

\item{target_pathway}{Exact pathway name (rowname in \code{auc_mat}).}

\item{period12}{Logical. 12-hr (TRUE) or 24-hr (FALSE, default).}

\item{use_violin}{Logical. Violin density (TRUE) or jittered dots (FALSE, default).}

\item{custom_zt}{Optional character vector to filter ZT time points.}
}
\value{
A ggplot2 object.
}
\description{
Mirrors \code{plot_gene_single()} but for pathway-level AUCell scores.
Shows per-cell AUC values (jittered dots or violin) per ZT time point,
per-ZT mean as orange points, and the fitted cosine curve in blue.
}
