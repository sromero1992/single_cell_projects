% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_pipeline}
\alias{run_pipeline}
\title{Run an ordered list of steps over an object}
\usage{
run_pipeline(x, steps, verbose = TRUE)
}
\arguments{
\item{x}{The starting object (sample list or Seurat object).}

\item{steps}{A list of steps (see above).}

\item{verbose}{Announce each step (default TRUE).}
}
\value{
The final object, with provenance recorded.
}
\description{
Each step is either a function (called as \code{fn(x)}) or a list
\verb{list(fn = <function>, args = <named list>)} called as
\code{do.call(fn, c(list(x), args))}. The object is threaded through in order.
}
\examples{
\dontrun{
data <- run_pipeline(paths, list(
  as_sample_list,
  function(x) qc_light(x, apply = FALSE),
  list(fn = detect_doublets, args = list(method = "both")),
  merge_samples,
  list(fn = integrate_data, args = list(method = "RunHarmony"))
))
}
}
