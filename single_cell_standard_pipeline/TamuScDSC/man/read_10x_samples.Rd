% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{read_10x_samples}
\alias{read_10x_samples}
\title{Load per-sample 10x/H5 matrices into a sample list (metadata-driven)}
\usage{
read_10x_samples(
  metadata,
  h5_dir,
  sample_col = "SampleID",
  h5_name = "sample_filtered_feature_bc_matrix.h5",
  min_cells = 5,
  probe = NULL
)
}
\arguments{
\item{metadata}{A data.frame with one row per sample and a \code{sample_col}.}

\item{h5_dir}{Directory containing one sub-folder per sample.}

\item{sample_col}{Column naming each sample (default "SampleID").}

\item{h5_name}{File name of the filtered matrix inside each sample folder.}

\item{min_cells}{Passed to \code{\link[Seurat:reexports]{Seurat::CreateSeuratObject()}}.}

\item{probe}{Optional list to enable probe summing: \code{list(h5_name=, feature=, probes_for_sum=, mapping=)}. NULL (default) skips all probe handling.}
}
\value{
A named list of per-sample Seurat objects.
}
\description{
Migrated from the STEP 2.1a loader in \verb{01_process_data.R}. Loops over the rows
of a metadata table, reads each sample's Cell Ranger H5, builds a Seurat
object, and broadcasts that sample's metadata row onto its cells (skipping
names reserved by Seurat / the pipeline). Optional probe summing for Flex/KO
designs is supported via \code{probe}.
}
