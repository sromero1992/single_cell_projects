% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doublets.R
\name{detect_doublets}
\alias{detect_doublets}
\title{Detect doublets on a sample list or a merged object}
\usage{
detect_doublets(
  x,
  method = NULL,
  rate = NULL,
  by = "SampleID",
  p = doublet_params(),
  diag_dir = NULL
)
}
\arguments{
\item{x}{A Seurat object or a list of per-sample Seurat objects.}

\item{method, rate}{Convenience overrides for \code{p$method} / \code{p$rate}.}

\item{by}{For a merged object: metadata column to split on before detecting
(per-sample detection). \code{NULL} runs on the whole object at once.}

\item{p}{A \code{\link[=doublet_params]{doublet_params()}} list.}

\item{diag_dir}{Directory for per-sample diagnostic plots (NULL to skip).}
}
\value{
The same type as \code{x}, with doublet columns added.
}
\description{
Writes the schema columns (\code{DF_score}, \code{DF_class}, \code{scDblFinder_score},
\code{scDblFinder_class}, \code{doublet_consensus}). Never removes cells — use
\code{\link[=apply_doublet_action]{apply_doublet_action()}} for that.
}
